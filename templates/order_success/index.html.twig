{% extends 'base.html.twig' %}

{% block title %}Commande validée ! | La Boutique Française{% endblock %}

{% block content %}
    
    <h2>Confirmation de commande</h2>
    <br>
    <p>
        Bonjour {{ order.user.firstname }} {{ order.user.lastname }},<br><br>
        Nous vous remercions pour votre commande n° <b>{{ order.reference }}</b>.<br>
        Vous allez recevoir une confirmation de commande par email à l'adresse : <b>{{ order.user.email }}</b>.
    <hr>
        Votre commande sera livrée par <b>{{ order.carrierName }}</b> à l'adresse suivante :<br>
        <b>{{ order.delivery|raw }}</b>
    <hr>


        {# Pour suivre votre commande, rendez-vous dans votre <a href="{{path('app_account_order')}}">compte</a>. #}
    </p>

    <div class="d-flex flex-row justify-content-start">
	    <h6 class="my-auto mr-5">Pour suivre votre commande, rendez-vous dans </h6>
            <a href="{{ path('app_account_order') }}" class="btn btn-outline-info btn-block my-auto w-25">Mes commandes</a>
    </div>
    
{% endblock %}
